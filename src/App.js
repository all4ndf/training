import React from "react";
import { withRouter } from "react-router-dom";
const App = () => {
  return (
    <div>
      <h1>App Js</h1>
      <button>Form1</button>
      <button>Form2</button>
    </div>
  );
};

export default withRouter(App);
